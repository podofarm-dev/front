/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.podofarm.xyz', // 실제 배포된 도메인
  generateRobotsTxt: true, // robots.txt 자동 생성
  sitemapSize: 5000, // 한 개의 sitemap에 포함할 URL 수 (기본값 5000)
};
